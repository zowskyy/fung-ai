# {{app_name}}

> {{app_idea}}

## Open in Android Studio
1. Open Android Studio
2. File → Open → select this folder
3. Wait for Gradle to sync (~1 min first time)
4. Click the green Run button

## Ask the AI to build more
Open the "Spread the fungus..." tab and say things like:
- "Add a score counter"
- "Make the button change color each tap"
- "Add a second screen"

The AI will write the code and explain each change.

Package: `{{package_name}}`
Min Android: API {{min_sdk}}
Theme: {{theme_color}}
